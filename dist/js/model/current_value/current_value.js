define(['app'], function (app) {
  var CurrentValue = Backbone.Model.extend({
    defaults: {
      label: 'My Total Stack Value',
      value: 24000
    },
    initalize: function() {

    }
  });

  return BullionType;
});